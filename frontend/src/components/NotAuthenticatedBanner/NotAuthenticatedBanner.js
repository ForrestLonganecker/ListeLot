import React from 'react';

import './NotAuthenticatedBanner.css';

const NotAuthenticatedBanner = () => (

  <div className="not-auth-banner-container">
    <h1 className="not-auth-site-title">ListeLot</h1>
  </div>
);

export default NotAuthenticatedBanner;